# Supabase — use the anon (public) key in the browser only
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=

# Optional: API base for fetch calls (defaults to same-origin /api via Vite proxy in dev)
# VITE_API_URL=http://localhost:3001
